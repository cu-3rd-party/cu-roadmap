import { PagePlaceholder } from "@/shared/ui";

// Base shell only — specialization restrictions editor lands here.
export default function RestrictionsPage() {
  return (
    <PagePlaceholder
      title="Ограничения"
      description="Ограничения дисциплин по специализациям."
    />
  );
}
