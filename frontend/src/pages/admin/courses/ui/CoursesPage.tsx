import { PagePlaceholder } from "@/shared/ui";

// Base shell only — the course CRUD table lands here.
export default function CoursesPage() {
  return (
    <PagePlaceholder
      title="Курсы"
      description="Управление каталогом дисциплин."
    />
  );
}
