import { PagePlaceholder } from "@/shared/ui";

// Base shell only — the trajectory editor lands here.
export default function TrajectoriesPage() {
  return (
    <PagePlaceholder
      title="Траектории"
      description="Управление образовательными траекториями."
    />
  );
}
