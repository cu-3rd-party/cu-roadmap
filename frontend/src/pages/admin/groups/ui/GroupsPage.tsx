import { PagePlaceholder } from "@/shared/ui";

// Base shell only — the box requirements editor lands here.
const BoxesPage = () => {
  return (
    <PagePlaceholder
      title="Коробки"
      description="Управление коробками и их требованиями."
    />
  );
};

export default BoxesPage;
