; frontend/ is a single-package pnpm workspace (pnpm-workspace.yaml: packages: - .),
; so adding deps to the workspace root is correct. Silence the root-check so tools
; like `shadcn add` (which run a plain `pnpm add`) work without the -w flag.
ignore-workspace-root-check=true
